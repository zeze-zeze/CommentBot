# --- OS ---
.DS_Store
Thumbs.db
ehthumbs.db
desktop.ini

# --- Editors / IDEs ---
.vscode/
.idea/
*.swp
*.swo
*~

# --- Claude Code local state ---
.claude/

# --- Secrets / local env (keys belong in the browser, never in the repo) ---
.env
.env.*
*.local

# --- Chrome extension packaging artifacts ---
# *.pem is the extension's private signing key — must never be committed
*.pem
*.crx
*.zip

# --- Node tooling (only if added later) ---
node_modules/
npm-debug.log*
yarn-error.log*

# --- Logs / temp ---
*.log
